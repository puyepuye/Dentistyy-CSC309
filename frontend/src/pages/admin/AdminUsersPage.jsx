import AppPlaceholderPanel from '../../components/AppPlaceholderPanel.jsx';

export default function AdminUsersPage() {
    return (
        <AppPlaceholderPanel
            title="Users"
            description="List regular users with filters, pagination, and suspension controls."
        />
    );
}
