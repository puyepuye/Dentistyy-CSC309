import AppPlaceholderPanel from '../../components/AppPlaceholderPanel.jsx';

export default function StaffScheduledPage() {
    return (
        <AppPlaceholderPanel
            title="Scheduled"
            description="Confirmed shifts and upcoming work windows will appear here."
        />
    );
}
