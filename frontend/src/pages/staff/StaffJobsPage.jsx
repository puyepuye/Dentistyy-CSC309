import AppPlaceholderPanel from '../../components/AppPlaceholderPanel.jsx';

export default function StaffJobsPage() {
    return (
        <AppPlaceholderPanel
            title="Jobs"
            description="Available postings you qualify for — list, filters, and pagination will connect to the API."
        />
    );
}
