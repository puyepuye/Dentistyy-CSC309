import AppPlaceholderPanel from '../../components/AppPlaceholderPanel.jsx';

export default function EmployerJobsPage() {
    return (
        <AppPlaceholderPanel
            title="Job postings"
            description="Create and manage postings — status, filters, and pagination to be wired to A2 routes."
        />
    );
}
